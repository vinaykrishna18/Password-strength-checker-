<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Password Strength Checker</title>
<style>
    body {
        font-family: Arial, sans-serif;
        background: #121212;
        color: #fff;
        margin: 0;
        padding: 20px;
    }
    h1, h2, h3 {
        text-align: center;
        color: cyan;
        text-shadow: 0 0 5px cyan;
    }
    /* Common section styles */
    .info-table, .tips-table, .checker-table {
        background: #1e1e1e;
        border-radius: 12px;
        padding: 20px;
        margin: 20px auto;
        width: 80%;
        box-shadow: 0 0 15px rgba(0, 255, 255, 0.3);
        opacity: 0;
        transform: translateY(30px);
        animation: fadeInUp 1s forwards;
    }
    /* Delay animations for each section */
    .info-table { animation-delay: 0.2s; }
    .tips-table { animation-delay: 0.5s; }
    .checker-table { animation-delay: 0.8s; }

    @keyframes fadeInUp {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    ul {
        list-style: none;
        padding-left: 0;
    }
    ul li {
        margin-bottom: 8px;
        font-size: 1rem;
    }
    /* arrow bullets for tips */
    .tips-table li::before {
        content: "→";
        color: cyan;
        margin-right: 8px;
        font-weight: bold;
    }
    /* checker container */
    .checker-container {
        text-align: center;
    }
    .password-box {
        position: relative;
        display: inline-block;
        width: 80%;
    }
    .password-box input {
        width: 100%;
        padding: 10px 35px 10px 10px;
        font-size: 1rem;
        border-radius: 8px;
        border: none;
        outline: none;
        box-sizing: border-box;
    }
    .toggle-password {
        position: absolute;
        right: 10px;
        top: 50%;
        transform: translateY(-50%);
        cursor: pointer;
        color: #888;
    }
    /* strength bar */
    #strength-bar {
        width: 80%;
        height: 8px;
        background: #444;
        border-radius: 8px;
        margin: 8px auto;
        overflow: hidden;
    }
    #strength-bar div {
        height: 100%;
        transition: width 0.4s ease, background 0.4s ease;
    }
    /* bullet points for password criteria */
    .criteria-list {
        list-style: none;
        padding-left: 0;
        text-align: left;
        display: inline-block;
        width: 80%;
        font-size: 0.9rem;
    }
    .criteria-list li::before {
        content: "•";
        margin-right: 8px;
        color: cyan;
        font-weight: bold;
    }
    button {
        margin-top: 10px;
        padding: 10px 15px;
        border: none;
        background: cyan;
        color: #000;
        font-weight: bold;
        border-radius: 8px;
        cursor: pointer;
    }
    button:hover {
        background: #00ffffaa;
    }
    #generated-password {
        margin-top: 10px;
        font-weight: bold;
        color: lightgreen;
    }
</style>
</head>
<body>

<h1>Know About Password</h1>
<div class="info-table">
    <p>Think twice before choosing your password—if it’s not strong enough, don’t be shocked to discover a hacker feasting in your account the next time you log in. Use our password checker to make sure your account stays secure and off-limits to intruders.</p>
</div>

<div class="tips-table">
    <h3>📌 Password Privacy Tips</h3>
    <ul>
        <li>Use at least 12+ characters for better security.</li>
        <li>Mix uppercase, lowercase, numbers & symbols.</li>
        <li>Never reuse passwords for important accounts.</li>
        <li>Change passwords regularly to reduce risk.</li>
        <li>Use a password manager to store them safely.</li>
    </ul>
</div>

<h2>Password Strength Checker</h2>
<div class="checker-table">
    <div class="checker-container">
        <div class="password-box">
            <input type="password" id="password" placeholder="Enter your password" />
            <span class="toggle-password" onclick="togglePassword()">👁</span>
        </div>

        <p id="strength-text">
            <span id="strength-label"></span> 
            <span id="score-text"></span>
        </p>

        <ul class="criteria-list" id="criteria-list">
            <li id="length-check">At least 8 characters</li>
            <li id="uppercase-check">Contains uppercase & lowercase</li>
            <li id="number-check">Contains a number</li>
            <li id="symbol-check">Contains a symbol</li>
        </ul>

        <div id="strength-bar"></div>

        <p style="margin-top:10px;">• You can also get a strong password here:</p>
        <button id="generate-btn">Generate Strong Password</button>
        <p id="generated-password"></p>
    </div>
</div>

<script>
const passwordInput = document.getElementById("password");
const strengthBar = document.getElementById("strength-bar");
const strengthLabel = document.getElementById("strength-label");
const scoreText = document.getElementById("score-text");
const generateBtn = document.getElementById("generate-btn");
const generatedPassword = document.getElementById("generated-password");

passwordInput.addEventListener("input", updateStrength);
generateBtn.addEventListener("click", generatePassword);

function updateStrength() {
    const password = passwordInput.value;
    const strength = checkStrength(password);

    strengthBar.innerHTML = "";
    const bar = document.createElement("div");
    const colors = [
        "linear-gradient(90deg, red, darkred)",
        "linear-gradient(90deg, orangered, orange)",
        "linear-gradient(90deg, gold, yellow)",
        "linear-gradient(90deg, yellowgreen, green)",
        "linear-gradient(90deg, limegreen, cyan)"
    ];
    const texts = [
        "Very Weak ❌",
        "Weak ⚠️",
        "Moderate 🙂",
        "Strong 💪",
        "Very Strong 🔥"
    ];

    bar.style.width = ((strength.score / 5) * 100) + "%";
    bar.style.background = colors[strength.score - 1] || "transparent";
    strengthLabel.textContent = texts[strength.score - 1] || "";
    scoreText.textContent = `(${strength.score}/5)`;

    strengthBar.appendChild(bar);

    // Update bullet colors
    document.getElementById("length-check").style.color = password.length >= 8 ? "lime" : "#ccc";
    document.getElementById("uppercase-check").style.color = (/[A-Z]/.test(password) && /[a-z]/.test(password)) ? "lime" : "#ccc";
    document.getElementById("number-check").style.color = /\d/.test(password) ? "lime" : "#ccc";
    document.getElementById("symbol-check").style.color = /[^A-Za-z0-9]/.test(password) ? "lime" : "#ccc";
}

function checkStrength(password) {
    let score = 0;
    if (password.length >= 8) score++;
    if (/[A-Z]/.test(password) && /[a-z]/.test(password)) score++;
    if (/\d/.test(password)) score++;
    if (/[^A-Za-z0-9]/.test(password)) score++;
    if (password.length >= 12) score++; // extra credit for long passwords
    return { score };
}

function generatePassword() {
    const length = 12;
    const charset = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789!@#$%^&*()_+";
    let password = "";
    for (let i = 0; i < length; i++) {
        password += charset.charAt(Math.floor(Math.random() * charset.length));
    }
    generatedPassword.textContent = password;
    passwordInput.value = password;
    updateStrength();
}

function togglePassword() {
    passwordInput.type = passwordInput.type === "password" ? "text" : "password";
}
</script>

</body>
</html>
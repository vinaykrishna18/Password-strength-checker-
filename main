<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<title>Password Strength Checker</title>
<style>
    body {
        font-family: 'Segoe UI', Arial, sans-serif;
        background: radial-gradient(circle at top left, #0a0a0a, #000428, #004e92);
        color: #fff;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 20px;
        background-attachment: fixed;
    }

    .container {
        max-width: 520px;
        width: 100%;
        background: rgba(255, 255, 255, 0.05);
        backdrop-filter: blur(12px);
        padding: 25px;
        border-radius: 20px;
        box-shadow: 0 0 30px rgba(0, 255, 255, 0.15);
        animation: fadeIn 1s ease-in-out;
    }

    h1, h2 {
        text-align: center;
        background: linear-gradient(to right, cyan, lightgreen);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        margin-bottom: 15px;
    }

    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(20px); }
        to { opacity: 1; transform: translateY(0); }
    }

    /* Cards */
    .card {
        background: rgba(255, 255, 255, 0.07);
        padding: 15px;
        border-radius: 12px;
        margin-bottom: 15px;
        border: 1px solid rgba(255, 255, 255, 0.15);
        box-shadow: 0 4px 15px rgba(0, 255, 255, 0.1);
        animation: layerFade 0.8s ease-in-out;
    }

    @keyframes layerFade {
        0% { transform: translateY(20px) scale(0.98); opacity: 0; }
        100% { transform: translateY(0) scale(1); opacity: 1; }
    }

    .card h3 {
        margin-top: 0;
        font-size: 1rem;
        background: linear-gradient(to right, #ff00cc, #3333ff);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .card p {
        font-size: 0.92rem;
        color: #ddd;
    }

    /* Password Privacy Tips */
    .tips-table ul {
        margin: 0;
        padding-left: 0;
        list-style: none;
    }

    .tips-table li {
        margin: 5px 0;
        padding: 6px 10px;
        border-radius: 6px;
        display: flex;
        align-items: center;
        gap: 6px;
        font-size: 0.88rem;
        background: rgba(255, 255, 255, 0.07);
        transition: 0.3s;
    }

    .tips-table li:hover {
        transform: scale(1.04);
        background: rgba(255, 255, 255, 0.15);
    }

    .bullet-icon {
        color: cyan;
        font-size: 0.85rem;
        flex-shrink: 0;
    }

    /* Password Input */
    .input-group {
        position: relative;
        display: flex;
        align-items: center;
    }

    input {
        width: 100%;
        padding: 10px 40px 10px 10px;
        font-size: 1rem;
        border-radius: 8px;
        border: none;
        outline: none;
        box-sizing: border-box;
    }

    .toggle-password {
        position: absolute;
        right: 10px;
        cursor: pointer;
        color: #aaa;
        font-size: 1.2rem;
    }

    /* Strength Bar */
    #strength-bar {
        width: 100%;
        height: 12px;
        background: #333;
        border-radius: 8px;
        margin: 10px 0;
        overflow: hidden;
    }

    #strength-bar div {
        height: 100%;
        transition: 0.4s ease;
    }

    #strength-text {
        font-weight: bold;
        margin: 5px 0;
    }

    #score-text {
        font-weight: normal;
        color: #ccc;
        font-size: 0.9rem;
    }

    /* Buttons */
    button {
        margin-top: 10px;
        padding: 10px 15px;
        border: none;
        background: linear-gradient(45deg, cyan, lime);
        color: #000;
        font-weight: bold;
        border-radius: 8px;
        cursor: pointer;
        transition: 0.3s;
    }

    button:hover {
        background: linear-gradient(45deg, #00ffffaa, #90ee90aa);
        transform: scale(1.05);
    }

    /* Generated Password */
    #generated-password {
        margin-top: 10px;
        font-weight: bold;
        color: lightgreen;
        text-align: center;
        word-break: break-word;
    }

    /* Checker list bullets */
    .bullets {
        padding-left: 0;
        list-style: none;
    }

    .bullets li {
        background: rgba(255, 255, 255, 0.05);
        margin: 6px 0;
        padding: 8px 12px;
        border-radius: 8px;
        display: flex;
        align-items: center;
        gap: 8px;
    }

    .bullets li::before {
        content: "✔";
        color: lime;
        font-weight: bold;
    }
</style>
</head>
<body>
<div class="container">
    <h1>Know About Password</h1>

    <!-- Why Strong Password Table -->
    <div class="card">
        <h3>🔐 Think Twice Before Choosing</h3>
        <p>
            Think twice before choosing your password—if it’s not strong enough, don’t be shocked to discover 
            a hacker feasting in your account the next time you log in. Use our password checker to make sure 
            your account stays secure and off-limits to intruders.
        </p>
    </div>

    <!-- Privacy Tips Table -->
    <div class="card tips-table">
        <h3>📌 Password Privacy Tips</h3>
        <ul>
            <li><span class="bullet-icon">→</span>Use at least 12+ characters for better security.</li>
            <li><span class="bullet-icon">→</span>Mix uppercase, lowercase, numbers & symbols.</li>
            <li><span class="bullet-icon">→</span>Never reuse passwords for important accounts.</li>
            <li><span class="bullet-icon">→</span>Change passwords regularly to reduce risk.</li>
            <li><span class="bullet-icon">→</span>Use a password manager to store them safely.</li>
        </ul>
    </div>

    <!-- Password Strength Checker Table -->
    <div class="card checker-table">
        <h2>Password Strength Checker</h2>

        <div class="input-group">
            <input type="password" id="password" placeholder="Enter your password" />
            <span class="toggle-password" id="toggle-password">👁️</span>
        </div>

        <p style="font-size: 0.85rem; color:#ccc;">You can also get a strong password here.</p>

        <div id="strength-bar"></div>
        <p id="strength-text">
            <span id="strength-label"></span> <span id="score-text"></span>
        </p>

        <ul class="bullets">
            <li>You can check the strength of your password.</li>
            <li>You can also generate secure passwords instantly.</li>
        </ul>

        <button id="generate-btn">Generate Strong Password</button>
        <p id="generated-password"></p>
    </div>
</div>

<script>
    const passwordInput = document.getElementById("password");
    const strengthBar = document.getElementById("strength-bar");
    const strengthLabel = document.getElementById("strength-label");
    const scoreText = document.getElementById("score-text");
    const generateBtn = document.getElementById("generate-btn");
    const generatedPassword = document.getElementById("generated-password");
    const togglePassword = document.getElementById("toggle-password");

    passwordInput.addEventListener("input", updateStrength);
    generateBtn.addEventListener("click", generatePassword);
    togglePassword.addEventListener("click", togglePasswordVisibility);

    function updateStrength() {
        const password = passwordInput.value;
        const strength = checkStrength(password);

        strengthBar.innerHTML = "";
        const bar = document.createElement("div");

        const colors = [
            "linear-gradient(90deg, red, darkred)",
            "linear-gradient(90deg, orangered, orange)",
            "linear-gradient(90deg, gold, yellow)",
            "linear-gradient(90deg, yellowgreen, green)",
            "linear-gradient(90deg, limegreen, cyan)"
        ];
        const texts = [
            "Very Weak ❌",
            "Weak ⚠️",
            "Moderate 🙂",
            "Strong 💪",
            "Very Strong 🔥"
        ];

        bar.style.width = ((strength.score / 5) * 100) + "%";
        bar.style.background = colors[strength.score - 1] || "transparent";

        strengthLabel.textContent = texts[strength.score - 1] || "";
        scoreText.textContent = `(${strength.score}/5)`;

        strengthBar.appendChild(bar);
    }

    function checkStrength(password) {
        let score = 0;
        if (password.length >= 8) score++;
        if (password.length >= 12) score++;
        if (/[A-Z]/.test(password) && /[a-z]/.test(password)) score++;
        if (/\d/.test(password)) score++;
        if (/[^A-Za-z0-9]/.test(password)) score++;
        return { score };
    }

    function generatePassword() {
        const length = 12;
        const lower = "abcdefghijklmnopqrstuvwxyz";
        const upper = "ABCDEFGHIJKLMNOPQRSTUVWXYZ";
        const numbers = "0123456789";
        const special = "!@#$%^&*()_+";
        const all = lower + upper + numbers + special;

        let password = [
            lower.charAt(Math.floor(Math.random() * lower.length)),
            upper.charAt(Math.floor(Math.random() * upper.length)),
            numbers.charAt(Math.floor(Math.random() * numbers.length)),
            special.charAt(Math.floor(Math.random() * special.length))
        ];

        for (let i = password.length; i < length; i++) {
            password.push(all.charAt(Math.floor(Math.random() * all.length)));
        }

        password = password.sort(() => Math.random() - 0.5).join("");

        generatedPassword.textContent = password;
        passwordInput.value = password;
        updateStrength();
    }

    function togglePasswordVisibility() {
        if (passwordInput.type === "password") {
            passwordInput.type = "text";
            togglePassword.textContent = "🫣";
        } else {
            passwordInput.type = "password";
            togglePassword.textContent = "👁️";
        }
    }
</script>
</body>
</html>